{
  "name": "firestore-rules-tests",
  "version": "1.0.0",
  "description": "Tests for Firestore security rules",
  "main": "index.js",
  "scripts": {
    "test": "jest --verbose --runInBand"
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^2.0.7",
    "jest": "^29.5.0"
  }
}
